"""
Setup script for the AI Voice Banking Assistant backend
"""
from setuptools import setup, find_packages

setup(
    name="banking-assistant",
    version="1.0.0",
    description="AI Voice Banking Assistant Backend",
    packages=find_packages(),
    python_requires=">=3.9",
    install_requires=[
        # Core dependencies are in requirements.txt
        # This is mainly for development installation
    ],
)









